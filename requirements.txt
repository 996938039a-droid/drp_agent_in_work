# DPR Agent — Python dependencies
# Install with: pip install -r requirements.txt

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Anthropic API ─────────────────────────────────────────────────────────────
# aiohttp is used directly (avoids anthropic SDK version conflicts)
aiohttp>=3.9.0

# ── Excel generation ──────────────────────────────────────────────────────────
openpyxl>=3.1.2

# ── Standard library (included in Python 3.10+, listed for clarity) ──────────
# asyncio    — built-in
# json       — built-in
# re         — built-in
# dataclasses — built-in (Python 3.7+)
# typing     — built-in

# ── Optional: for .env file support ──────────────────────────────────────────
python-dotenv>=1.0.0
